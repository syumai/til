version: "2"
sql:
  - engine: sqlite
    queries: query.sql
    schema: schema.sql
    gen:
      go:
        package: model
        out: model
        emit_json_tags: true
        emit_result_struct_pointers: true

